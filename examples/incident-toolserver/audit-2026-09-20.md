report-format: v3
scope:               examples/incident-toolserver/broken/toolserver.mjs
principles-revision: 1
date:                2026-09-20
commit:              c3293278e50a63fef29fce40a1a47f0bbca783a9

## Declared architecture

No `AGENTS.md`, `CLAUDE.md`, or boundary config exists in `examples/incident-toolserver/` or `examples/incident-toolserver/broken/` declaring a source-truth layer or layer direction, and no builder was present in this run to answer the declaration questions. Per the audit procedure, this run proceeds calibrated to adopted principles only: P1 — Source-truth layer and P2 — One-way layering are not scored (no declared or practiced layering exists in a single flat script), and P7 — Contracted outputs, P8 — State-first lineage, and P9 — Golden baseline + explain-the-drift omissions are reported once as an advisory adoption gap rather than as per-function violations (see finding 7).

## Principle glossary

P3 — Confidence + provenance   P6 — Maturity vocabulary
P4 — Quarantined fakery        P7 — Contracted outputs
P8 — State-first lineage       P9 — Golden baseline + explain-the-drift
spine — null-result expressibility

## Findings

| Path | Line | Function | Issue | Suggested Fix | Principle |
| ---- | ---- | -------- | ----- | ------------- | --------- |
| `examples/incident-toolserver/broken/toolserver.mjs` | 25-27 | `spawnWorker` | Stub returns a hardcoded `success: true` payload (`workerId: "worker-1"`, `status: "ready"`) with no worker actually spawned, and no label marking the value as mock; it flows straight into the shared `results` array used for the aggregate health report. | Tag the return value as mock (e.g. a `derivedFromMock`-style marker or a `status: "mock"` field) and exclude it from the success aggregation unless the caller explicitly opts in. | P4 — Quarantined fakery |
| `examples/incident-toolserver/broken/toolserver.mjs` | 29-31 | `orchestrateTasks` | Same pattern as `spawnWorker`: hardcoded `success: true` / `taskId: "task-1"` / `status: "scheduled"` with no task actually scheduled, unlabelled, merged into the aggregate. | Same as above — mark and quarantine the mock payload. | P4 — Quarantined fakery |
| `examples/incident-toolserver/broken/toolserver.mjs` | 33-35 | `storeMemory` | Same pattern: hardcoded `success: true` / `status: "persisted"` with nothing stored, unlabelled, merged into the aggregate. | Same as above — mark and quarantine the mock payload. | P4 — Quarantined fakery |
| `examples/incident-toolserver/broken/toolserver.mjs` | 39-43 | (module top level, `results`) | `hashText`/`countWords` (real) and `spawnWorker`/`orchestrateTasks`/`storeMemory` (mock) are mapped into one `results` array and later counted identically via `r.success`; nothing on any result records whether it came from a real computation or a hardcoded stub, so the two kinds of "success" are indistinguishable downstream. | Attach an origin/confidence field to each tool's result (e.g. `source: "real"` vs `source: "mock"`) and propagate it into the aggregate rather than collapsing to a single boolean. | P3 — Confidence + provenance |
| `examples/incident-toolserver/broken/toolserver.mjs` | 45-52 | (module top level, health report) | The health report's own logic declares a failure/degraded branch (`r.success ? "success" : "FAILED"`, `succeeded === results.length ? "operational" : "degraded"`), but the three stub tools are hardcoded to unconditionally return `success: true`, so that branch can never fire for the 3/5 tools that do nothing — the report structurally cannot say "these tools didn't actually run." | Give the stub tools an honest not-implemented/error result (or have them throw/return `success: false`) so the existing degraded/FAILED path can actually be reached and the health status reflects reality. | spine — null-result expressibility |
| `examples/incident-toolserver/broken/toolserver.mjs` | 50-52 | (module top level, health report) | `system health: operational (5/5 tools succeeded)` is printed when 3 of the 5 "tools" are unbuilt stubs; no `mock` / `not-implemented` maturity label is applied anywhere in the file to `spawnWorker`, `orchestrateTasks`, or `storeMemory`, so the report claims a fully operational, current system. | Apply the maturity vocabulary to each stub (comment or field marking them `mock`/`not-implemented`) and have the health calculation weight or exclude non-`current` tools rather than counting them as successes. | P6 — Maturity vocabulary |
| `examples/incident-toolserver/broken/` | — | — | (Advisory, not a violation) No declared ruleset/architecture exists for this scope, so P7 — Contracted outputs, P8 — State-first lineage, and P9 — Golden baseline + explain-the-drift are adopted nowhere here: none of the six output-producing units (`hashText`, `countWords`, `spawnWorker`, `orchestrateTasks`, `storeMemory`, the aggregate health report) has a versioned/validated output contract, records the inputs needed to reproduce its output, or is pinned against a golden baseline. Reported once per the calibration rule rather than as six separate omission violations. | Declare source-truth/output conventions for this example (or via `plumb-line-bootstrap`'s declaration-only entry) before treating a repeat of this gap as a confirmed violation. | P6 — Maturity vocabulary |

## Coverage map

- `examples/incident-toolserver/broken/toolserver.mjs` — read

coverage: 1/1 files read, 0 partial, 0 not-read (100%)
scope note: findings are drawn from the read set only; a not-read file with no
finding is not a clean file. This audit does not claim completeness. Per
instruction, `examples/incident-toolserver/broken/VIOLATIONS.md`,
`examples/incident-toolserver/README.md`, and `examples/incident-toolserver/instrumented/`
were deliberately excluded from scope and not read.

format-validation: scripts/check_report_format.py — clean

7 findings: 6 violations, 1 needs-review
