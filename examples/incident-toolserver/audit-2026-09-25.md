report-format: v3
scope:               examples/incident-toolserver/broken/toolserver.mjs
principles-revision: 1
date:                2026-09-25
commit:              b45ab85e5ac711998a6de70a4de222bf942c115c

P1 — Source-truth layer      P4 — Quarantined fakery   P7 — Contracted outputs
P2 — One-way layering        P5 — Injectable priors    P8 — State-first lineage
P3 — Confidence + provenance P6 — Maturity vocabulary  P9 — Golden baseline + explain-the-drift
spine — null-result expressibility

Declared architecture: none found for this scope. No builder was present, no architecture was supplied in the invocation, and no ruleset or boundary config covers `examples/incident-toolserver/` (the repo-root `AGENTS.md` governs the plumb-line repo itself and declares no source-truth layer or layer direction for this example). The audit therefore proceeds calibrated to adopted principles only, infers no source-truth layer, and its P1 — Source-truth layer and P2 — One-way layering coverage is `partial`.

| Path | Line | Function | Issue | Suggested Fix | Principle |
| ---- | ---- | -------- | ----- | ------------- | --------- |
| `examples/incident-toolserver/broken/toolserver.mjs` | 25-35 | `spawnWorker`, `orchestrateTasks`, `storeMemory` | violation: three stubs return fabricated payloads (`workerId: "worker-1"`, `taskId: "task-1"`, `status: "persisted"`) with no mock marker in the data; the "stub" label exists only in a source comment (lines 21-23), invisible to the report logic that consumes them | label stub results in the payload itself (e.g. `mock: true` / `implemented: false`) and exclude them from the status report unless explicitly opted in | P4 — Quarantined fakery |
| `examples/incident-toolserver/broken/toolserver.mjs` | 41-52 | module top level (status report) | violation: the aggregation counts stub `success: true` exactly like real tool results, so mock outcomes are laundered into `system health: operational (5/5 tools succeeded)`; the real/stub distinction is lost at the aggregate | carry an origin marker per result and have the report count only real executions (e.g. `2/2 real succeeded, 3 not-implemented`) | P3 — Confidence + provenance |
| `examples/incident-toolserver/broken/toolserver.mjs` | 26, 30, 34 | `spawnWorker`, `orchestrateTasks`, `storeMemory` | violation: stubs claim completed states (`ready`, `scheduled`, `persisted`) for work that never happened; the output asserts current maturity for mock code | return the maturity vocabulary term (`mock` / `not-implemented`) instead of a success-shaped status | P6 — Maturity vocabulary |
| `examples/incident-toolserver/broken/toolserver.mjs` | 25-35, 46-52 | `spawnWorker`, `orchestrateTasks`, `storeMemory`; status report | violation: the stubs can only return `success: true`, while the consuming report practices a failure outcome (`FAILED`, `degraded`); there is also no "not run / inconclusive" outcome, so an unexecuted tool collapses into success | give each tool a reachable failure/not-implemented result and give the report a third state (e.g. `unverified`) distinct from `operational` and `degraded` | spine — null-result expressibility |
| `examples/incident-toolserver/broken/toolserver.mjs` | 51 | module top level (status report) | needs-review: the health rule `succeeded === results.length` hardcodes an all-or-nothing judgment for what counts as `operational` | lift the health threshold/policy to named, versioned config reported alongside the verdict | P5 — Injectable priors |
| `examples/incident-toolserver/broken/toolserver.mjs` | 45-52 | module top level (status report) | needs-review (adoption gap): the status report records no lineage (which tools ran for real, input used, run time, code version); lineage is neither declared nor practiced anywhere in scope | record per-result execution evidence and run conditions with the report | P8 — State-first lineage |
| `examples/incident-toolserver/broken/toolserver.mjs` | — | — | needs-review (adoption gap): no tool-result or report shape has a version constant, validator, or canonical key list; contracts are adopted nowhere in scope | define a versioned result schema with a validator that rejects success claims lacking execution evidence | P7 — Contracted outputs |
| `examples/incident-toolserver/broken/toolserver.mjs` | — | — | needs-review (adoption gap): no golden baseline pins the status report output; baselines are adopted nowhere in scope | pin the report output in a baseline so a change in health verdict must be explained | P9 — Golden baseline + explain-the-drift |
| `examples/incident-toolserver/broken/toolserver.mjs` | — | — | needs-review (advisory): no declared source-truth layer or layer direction for this example; the project's rules exist only as vibes, so P1 — Source-truth layer and P2 — One-way layering could not be checked | declare the architecture (e.g. via `plumb-line-bootstrap` declaration-only entry) | P6 — Maturity vocabulary |

| Output | Provenance (P3 — Confidence + provenance) | Confidence (P3 — Confidence + provenance) | Lineage (P8 — State-first lineage) | Contract (P7 — Contracted outputs) | Null-expressible (spine — null-result expressibility) | Baseline (P9 — Golden baseline + explain-the-drift) |
| ------ | ---------- | ---------- | ------- | -------- | ---------------- | -------- |
| `hashText` result | partial — `tool` name only | NO | partial — digest reproducible from input, input not recorded | NO — adoption gap | NO — always `success: true` (throws on bad input) | NO — adoption gap |
| `countWords` result | partial — `tool` name only | NO | partial — count reproducible from input, input not recorded | NO — adoption gap | NO — always `success: true` | NO — adoption gap |
| `spawnWorker` result | NO — stub origin unmarked (P4 — Quarantined fakery) | NO | NO — nothing executed, fabricated id | NO — adoption gap | NO — stub, always success (spine — null-result expressibility) | NO — adoption gap |
| `orchestrateTasks` result | NO — stub origin unmarked (P4 — Quarantined fakery) | NO | NO — nothing executed, fabricated id | NO — adoption gap | NO — stub, always success (spine — null-result expressibility) | NO — adoption gap |
| `storeMemory` result | NO — stub origin unmarked (P4 — Quarantined fakery) | NO | NO — nothing stored, fabricated key | NO — adoption gap | NO — stub, always success (spine — null-result expressibility) | NO — adoption gap |
| status report (`system health` line) | NO — real/stub distinction dropped (P3 — Confidence + provenance) | NO | NO — adoption gap (P8 — State-first lineage) | NO — adoption gap | partial — `degraded` exists, no not-run/inconclusive state | NO — adoption gap |

```
file: examples/incident-toolserver/broken/toolserver.mjs — read
coverage: 1/1 files read, 0 partial, 0 not-read (100%)
scope note: findings are drawn from the read set only; a not-read file with no
finding is not a clean file. This audit does not claim completeness. The demo
files examples/incident-toolserver/broken/VIOLATIONS.md,
examples/incident-toolserver/README.md, and everything under
examples/incident-toolserver/instrumented/ were deliberately excluded from scope
and not read. P1 — Source-truth layer and P2 — One-way layering coverage is
partial because no architecture is declared for this scope.
```

format-validation: scripts/check_report_format.py — clean

9 findings: 4 violations, 5 needs-review
