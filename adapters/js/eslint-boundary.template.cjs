// Generated by plumb-line. Enforces one-way layering (Principle 2).
// Bootstrap replaces __ZONES__ with the project's layers.
// Zone semantics (easy to invert): target = the layer doing the importing;
// from = the layer it must NOT import. One entry per forbidden (lower imports
// higher) pair, e.g. { target: "./src/data", from: "./src/ui" }.
module.exports = {
  rules: {
    "import/no-restricted-paths": ["error", { zones: __ZONES__ }],
  },
};
