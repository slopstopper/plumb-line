# Dependencies
node_modules/

# Test coverage output
coverage/
.coverage
htmlcov/
coverage.xml

# Python caches & build artifacts
__pycache__/
*.pyc
.venv/
.pytest_cache/
.import_linter_cache/
*.egg-info/
build/
dist/

# OS cruft
.DS_Store

# Local tooling state (not for publication)
.claude/
.superpowers/
.remember/

# Internal build docs — kept private, not published.
# Only ADRs (docs/adr/) are the durable planning artifact in the repo;
# brainstorm specs and implementation plans stay local.
docs/plans/
docs/specs/
docs/superpowers/
