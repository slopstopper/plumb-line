# Direct test/build dependencies for plumb-line CI.
# Recompile the hashed lock after any change:
#   uv pip compile requirements-test.in --generate-hashes \
#       --python-version 3.11 -o requirements-test.txt
# The floor is Python 3.11 (see SUPPORT.md), so a single flat resolution covers
# every supported runtime — no universal marker split, no manual reconciliation.
pytest==9.1.1
pytest-cov==7.1.0
ruff==0.15.22
import-linter==2.13
build==1.5.0
requests
httpx
pandas
numpy
